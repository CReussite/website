# Environnement (clés secrètes — ne jamais commiter)
backend/.env
backend/**/.env
.env

# Railway CLI (config locale générée par `railway link`)
.railway/

# Render (config locale)
.render/

# Dépendances Node
node_modules/
backend/node_modules/

# PDFs réels (remplacent les placeholders — à ne pas commiter si volumineux)
# Désactivé : les placeholders de test sont commitables
# backend/assets/*.pdf

# Dossiers internes
.vscode/
.claude/
modif/
recommandation/

# MCP config (contient potentiellement des tokens — ne pas commiter)
.mcp.json

# OS
.DS_Store
Thumbs.db
