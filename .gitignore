# Environnement (clés secrètes — ne jamais commiter)
backend/.env
backend/**/.env
.env

# Render (config locale)
.render/

# Dépendances Node
node_modules/
backend/node_modules/

# PDFs produit — stockés dans Supabase Storage, ne pas commiter
backend/assets/*.pdf

# Dossier source des PDFs
pdf/

# Documentation opérationnelle locale (contient des infos sensibles)
CLAUDE.md

# Dossiers internes
.vscode/
.claude/
modif/
recommandation/
pub/
todo/
caroussel/

# MCP config (contient potentiellement des tokens — ne pas commiter)
.mcp.json

# OS
.DS_Store
Thumbs.db
